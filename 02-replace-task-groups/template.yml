# template.yml
steps:
- script: |
   echo First Step
   echo ${{parameters.message}}'
  displayName: 'First Step'
- script: |
   echo Second Step
   echo ${{parameters.message}}'
  displayName: 'Second Step'
